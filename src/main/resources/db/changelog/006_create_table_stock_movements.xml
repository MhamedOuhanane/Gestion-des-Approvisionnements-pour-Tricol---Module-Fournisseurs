<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <changeSet id="006" author="M'hamed">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="stock_movements"/>
            </not>
        </preConditions>
        <sql>
            CREATE TYPE stockMovement_type AS ENUM('ENTRANCE', 'EXIT', 'ADJUSTMENT');
        </sql>

        <createTable tableName="stock_movements">
            <column name="uuid" type="uuid">
                <constraints primaryKey="true" unique="true" nullable="false"/>
            </column>
            <column name="date" type="TIMESTAMP" defaultValueComputed="now()">
                <constraints nullable="false"/>
            </column>
            <column name="quantity" type="INT" defaultValue="0">
                <constraints nullable="false"/>
            </column>
            <column name="type" type="stockMovement_type" defaultValue="ENTRANCE">
                <constraints nullable="false"/>
            </column>
            <column name="product_id" type="uuid">
                <constraints nullable="false"/>
            </column>
        </createTable>
        <addForeignKeyConstraint
                baseTableName="stock_movements"
                baseColumnNames="product_id"
                constraintName="fk_stock_movements_products"
                referencedTableName="products"
                referencedColumnNames="uuid"
                onDelete="CASCADE"/>
    </changeSet>

</databaseChangeLog>